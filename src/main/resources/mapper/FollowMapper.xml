<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kok.mapper.FollowMapper">
    <select id="selectPopularCompany">
        SELECT c.user_id,
               c.company_name,
               c.company_info,
               c.company_url,
               COUNT(f.id) AS follow_count
        FROM tbl_company c
                 LEFT JOIN tbl_follow f ON c.user_id = f.company_id
        WHERE f.follow_status = 'active'
        GROUP BY c.user_id, c.company_name, c.company_info, c.company_url
        ORDER BY follow_count DESC
        LIMIT 6;
    </select>
</mapper>