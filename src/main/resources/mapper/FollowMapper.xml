<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kok.mapper.FollowMapper">
    <insert id="insertFollow">
        insert into tbl_follow (member_id, company_id)
        values (#{memberId}, #{companyId})
    </insert>

    <delete id="deleteFollow">
        delete from tbl_follow
        where member_id = #{memberId}
        and company_id = #{companyId}
    </delete>

    <select id="isFollowing" resultType="boolean">
        select count(*) > 0
        from tbl_follow
        where member_id = #{memberId}
        and company_id = #{companyId}
    </select>

    <select id="countFollowersByCompanyId" resultType="int">
        select count(*)
        from tbl_follow
        where company_id = #{companyId}
        and follow_status = 'active'
    </select>
</mapper>