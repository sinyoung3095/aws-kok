<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kok.mapper.CommunityReplyMapper">
    <!-- 대댓글 작성 -->
    <insert id="insertReply">
        insert into tbl_reply (reply_content, member_id, comment_id)
        values (#{replyContent}, #{memberId}, #{commentId})
    </insert>

    <!-- 대댓글 수정 -->
    <update id="updateReply">
        update tbl_reply
        set reply_content = #{commentContent}, updated_datetime = now()
        where id = #{id}
    </update>

    <!-- 대댓글 삭제 -->
    <update id="deleteReply">
        update tbl_reply
        set reply_status = 'inactive'
        where id = #{id}
    </update>
</mapper>