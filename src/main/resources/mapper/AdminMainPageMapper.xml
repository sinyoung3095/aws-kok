<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kok.mapper.AdminMainPageMapper">
<!--  회원 - 평균 체험 지원 수  -->
    <select id="memberExperienceRequestAverage" resultType="com.example.kok.dto.AdminMainPageDTO">
        select (
                (select count(*)::numeric from tbl_request_experience)
                    / nullif((select count(*) from tbl_member), 0)
               )::numeric(10, 2) as memberRequestAvg;
    </select>
</mapper>